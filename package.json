{
  "name": "jarvis-ai",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "node scripts/dev-server.mjs",
    "start:vercel": "npx -p vercel vercel dev --listen 3000",
    "start:react": "react-scripts start",
    "build": "react-scripts build",
    "analyze": "npx source-map-explorer 'build/static/js/*.js'",
    "test": "react-scripts test"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version"
    ]
  }
}
